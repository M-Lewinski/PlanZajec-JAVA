USE PlanZajec;

DELIMITER $$
DROP FUNCTION IF EXISTS `dropUser`$$
CREATE FUNCTION dropUser(login VARCHAR(30),host VARCHAR(30))
RETURNS BOOLEAN
DETERMINISTIC
BEGIN
DROP USER login@host;
RETURN (TRUE);
END$$
DELIMITER ;
